import { FC, Dispatch, SetStateAction } from "react";
import { NavType } from "../../Navigation";
export declare const ToolkitNavigationProvider: FC;
export declare const useNavTypes: () => {
    navTypes: NavType[];
    setNavTypes: Dispatch<SetStateAction<NavType[]>>;
};
