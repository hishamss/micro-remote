import React, { FC } from "react";
import "wicg-inert";
import { UITKTheme } from "@uitk/themes";
interface ToolkitProps {
    appId?: string;
    theme?: UITKTheme;
    spacing?: boolean;
    grid?: boolean;
    children?: React.ReactNode;
}
export declare const Toolkit: FC<ToolkitProps>;
export default Toolkit;
