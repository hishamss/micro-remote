import React, { FC } from "react";
import { NavigationConfig } from "../Navigation/types";
export interface HeaderProps {
    /**
     * Logo content slot.
     */
    logoContent: React.ReactNode;
    /**
     * Product Name
     */
    productName: string;
    /**
     * Product Link
     */
    productLink: string;
    /**
     * Config for global navigation.
     */
    globalNavigation?: NavigationConfig;
    /**
     * Config for horizontal navigation
     */
    horizontalNavigation?: NavigationConfig;
    /**
     * Hook that allows for subscription to the current route. Should return the
     * route. This is required if you specify a navigation config that uses client
     * side routing.
     */
    useLocation?: () => string;
    /**
     * Optional classname applied to component root.
     */
    className?: string;
    /**
     * Optional config to configure a skip link when tabbing the header
     */
    skipLink?: {
        /**
         * text to use for the skip link
         */
        text?: string;
        /**
         * css id of the element that focus should shift to when skip link is
         * pressed
         */
        id: string;
    };
    /**
     * Disable Skip Link
     */
    removeSkipLink?: boolean;
}
export declare const Header: FC<HeaderProps>;
export default Header;
