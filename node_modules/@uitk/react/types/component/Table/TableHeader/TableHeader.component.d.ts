import React from "react";
import { OnSortFunction } from "../types";
export declare const TableHeader: import("styled-components").StyledComponent<React.FC<{
    className?: string;
    captionId: string;
    onSort?: OnSortFunction;
}>, import("styled-components").DefaultTheme, {}, never>;
