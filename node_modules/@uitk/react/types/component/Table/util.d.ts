import { ITableData, IExpandableContent } from "./types";
/**
 * We have to throw an exception if expandable content is defined but these
 * config values are omitted to ensure a11y.
 */
declare type requiredConfigKeys = "getCloseButtonAriaLabel" | "getExpandButtonAriaLabel";
export declare const getMissingConfigHandler: (key: requiredConfigKeys) => () => never;
export declare const getAriaLabel: (rowData: ITableData, prefix: string, suffix: (rowData: ITableData) => string) => string;
/**
 * This just sets default values for the localisable labels so that we don't
 * have to add conditional code at the point where the labels are used.
 */
export declare const getNormalisedExpandableContentConfig: (expandableContent?: IExpandableContent) => IExpandableContent;
export {};
