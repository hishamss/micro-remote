import React, { FC } from "react";
import { ITableData } from "./types";
interface IExpandRowButtonProps extends React.ComponentProps<"button"> {
    onPress: () => void;
    expanded: boolean;
    expandableContentId: string;
    refInstance: HTMLButtonElement;
    rowData: ITableData;
}
export declare const ExpandRowButton: FC<IExpandRowButtonProps>;
export {};
