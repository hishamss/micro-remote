import React, { KeyboardEvent } from "react";
import { IDropdownItem } from "@component";
interface DropdownMultiGroupProps {
    dropdownGroup: IDropdownItem;
    selectedItems: IDropdownItem[];
    onSelectItems: (items: IDropdownItem[], selectedItems: IDropdownItem[]) => void;
    onFocus: (focusIndex: number) => void;
    onKeyDownGroupItem: (event: KeyboardEvent<HTMLInputElement>, index: number) => void;
    getItemRef: (itemId: string | number) => React.MutableRefObject<HTMLInputElement>;
    flatItems: IDropdownItem[];
    rootId: string;
    selectGroupLabel: string;
}
export declare const DropdownMultiGroup: import("styled-components").StyledComponent<React.FC<DropdownMultiGroupProps>, import("styled-components").DefaultTheme, {}, never>;
export {};
