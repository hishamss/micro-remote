import React, { FC } from "react";
export interface SelectProps extends React.ComponentProps<"select"> {
    /**
     * Class name applied to the native select element
     */
    className?: string;
    /**
     * Whether the select input is in error state
     */
    error?: null | string;
    /**
     * Select children
     */
    children?: React.ReactNode;
    /**
     * Ref that will be bound to the underlying select element
     */
    selectRef?: React.MutableRefObject<HTMLSelectElement>;
    /**
     * Optional caret icon override
     */
    caretIcon?: React.ReactNode;
}
export declare const Select: FC<SelectProps> & UITKComponent;
export default Select;
